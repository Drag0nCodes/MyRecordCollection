import { type Record } from '../types';

// Records and tags are now provided by the backend API. Keep an empty
// sampleRecords placeholder so TypeScript types stay consistent.
export const sampleRecords: Record[] = [];

export const allTags: string[] = [];